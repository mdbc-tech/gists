name: SSO
version: 0.1.3
patches:
  lms-env-features: |
    #"ENABLE_COMBINED_LOGIN_REGISTRATION" = true,
    "ENABLE_THIRD_PARTY_AUTH" = true,
    "SOCIAL_AUTH_OAUTH_SECRETS": { 
    "azuread-oauth2":
    "" }